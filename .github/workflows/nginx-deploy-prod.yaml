name: Deploy Nginx Config to Prod

on:
  push:
    paths:
      - "nginx-config/prod.conf"
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up SSH key
      run: |
        echo "${{ secrets.SSH_PRIVATE_KEY }}" > key.pem
        chmod 600 key.pem

    - name: Copy Nginx config file to Prod EC2
      run: |
        scp -i key.pem -o StrictHostKeyChecking=no nginx-config/prod.conf ubuntu@${{ secrets.PROD_HOST }}:/tmp/
        
        ssh -i key.pem -o StrictHostKeyChecking=no ubuntu@${{ secrets.PROD_HOST }} << 'EOF'
          sudo mv /tmp/prod.conf /etc/nginx/conf.d/
          sudo nginx -t && sudo systemctl reload nginx
        EOF
